# meteor-catch-mind

## Install Meteor

window: `npm install -g meteor`
linux: `curl https://install.meteor.com/ | sh`

## Setup

`meteor npm install`

## Run

`meteor run`

go to http://localhost:3000
